# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
/build

# environment — never commit real credentials
.env
.env*.local
.env.ship

# local SQLite database (dev)
*.db
*.db-journal
*.db-wal
*.db-shm

# local media uploads (dev)
/media

# vanilla Minecraft block assets for the Litematica viewer — Mojang IP, never
# committed; fetched at dev/build time by scripts/fetch-mc-assets.mjs
/public/litematic

# seed-generated banner images (recreated by `pnpm seed`)
/src/seed/assets/*.png

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/coverage
/.idea/*
!/.idea/runConfigurations
